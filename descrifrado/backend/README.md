# Backend for Descifrado

The Backend would be responsible for handling the database and sending the queried data to the frontend application. The infrastructure would be abstracted out from the user.
